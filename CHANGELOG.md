# Changelog

Ninlil Runtimeの利用者に影響する変更をこのファイルへ記録します。日付付きreleaseが始まるまでは、開発中の変更を`Unreleased`へまとめます。

## Unreleased

### Added

- **R4 SX1262 control-plane host candidate（P1 再修正; 独立監査待ち）**（[docs/28](docs/28-r4-sx1262-control-plane-backend.md); [ADR-0008](docs/adr/0008-r4-sx1262-control-plane-backend.md)）: DS Rev2.2 / driver v2.3.2 / SWSD003 v2.3.0 pin。BUSY=`delay_us` poll + overflow-safe `ceil` max_polls + frozen clock cap + **interval_us≤timeout_us**。write 後 **GetStatus verify**; mid-init accepted cmd_status **`{0,2}` only**（RFU=1 / TX_DONE=6 reject; 3/4/5 fail; 初回 reset 後のみ accepted 非強制）。**post_guard+post-BUSY 完了後にのみ rx[1] decode**。closed **frame schema**（exact len/params）。ESP SPI=`queue_trans`+`get_trans_result` finite + pending drain/REBOOT_REQUIRED。GPIO: outputs→safe RESET high/ANT inactive（**polarity config**）→inputs。RF ban gate: ALT_TX 全 reject + ports authority TU allowlist。docs/07 1:1。public ABI 非変更。**R4 complete / RF/HIL/legal 非主張。**
- **R1 ninlil_radio_hal host candidate**（[docs/23](docs/23-usb-radio-boundary.md) §9 / §10.2 R1; [docs/05](docs/05-security-and-compliance.md); [ADR-0003](docs/adr/0003-radio-usb-dependency-direction.md)）: production-private `src/radio/radio_hal.{h,c}` in `ninlil_runtime_private`（ESP-IDF source authority 共有）。**sole** physical TX entry `ninlil_radio_hal_transmit_with_permit` only（beacon/ACK/retry/relay/diagnostic 例外入口なし）。order: alias/args → **edge+permit+live+digest 全 authority 必須**（いずれか clear/NULL → default-deny TX 0）→ monotonic seq → call-entry dual local plan（working+seal; ≤256 frame）→ structural → live §9.3 → **required** digest(working only) → plan==seal → validate(working only) → plan==seal → consume(working only; **last authority callback**) → plan==seal → watermark **seal** seq → local plan==seal only（no external digest/permit）→ edge **exactly once on seal only**。trusted time は HAL 段ではなく **R2 permit context** 内（validate/consume の ctx clock; **No caller now_ms**）。local one-shot は `sequence <= last_consumed_seq` 全拒否（10→11→10 replay edge 0）; `last==UINT64_MAX` 後 SEQ_EXHAUSTED。full §9.3 snapshot fields 非簡略化。pointer alias（permit/frame/out_error↔HAL）fail-closed。structural: zero ID / reserved / zero core PHY（Japan 数値 authority 非偽装）。host spy + CTest `radio_hal_r1` + gate（mutation self-test + production nm）。heap/VLA 禁止; public ABI 非変更。**Does not implement R2 authority / R4 SX1262 / R6 wire / Japan profile / legal / RF HIL / production radio complete。 **No caller now_ms** — NIN-CMP-003 time is ctx/R2 permit-ops clock only（HAL は compliance time を権威としない）。 consume は last authority callback；post-consume は local integrity のみ。 **R1 series complete ではない**（host candidate）。compile ≠ HIL。**
- **U4 logical session host candidate**（[docs/23](docs/23-usb-radio-boundary.md) §5.5–§5.6.2 / §7 / §8 / §10.1 U4）: production-private `src/transport/logical_session.{h,c}` が **sole owner** として actual U3 `control_session`（epoch claim / tracked TX / pump / take_rx / tx_resolve）と actual NCG1/NCL1 codec を composition。外部 API は init/bind/**step**/snapshot/unbind のみ（U3 ハンドル非露出）。5 状態・Controller/Cell role・TX/RX sequence epoch・active gen/cookie・last_issued_gen burn（cancel 後も非再利用）・request allocator・HELLO/PING inflight≤8・continuity notice 固定 1 slot・fixed TX action queue・monotonic liveness（PING inflight≤1 / dispatch slack / PONG miss3 / HELLO retry 無制限 backoff≤5000ms）・CTRL_ERROR sliding 1s×8 / 応答禁止 / logical reject 連続32 fence・cookie CSPRNG（最大8 redraw、jitter 源と分離・fallback 禁止）・raw accept commit は `tx_resolve==RAW_ACCEPTED_CURRENT_EPOCH` のみ。RX exact pipeline（framing→stream→UINT32_MAX→sequence、SBR/BOOTSTRAP 限定 peek、通常 path は semantic→reserved step7 分離）。sibling `spec/vectors/logical-session-u4-v1.json` + independent `tools/logical_session_u4_vector_gen.py`（engine required **38** = ncl1 required 46 − pure-codec 8）+ structural `logical_session_u4_gate` + host CTest `logical_session_u4`（各 ID 固有 assertion）。object ceiling = U3 embed + U4 exclusive 8192（actual sizeof 静的 assert / gate）。**Does not claim U4 complete / USB series complete / HIL / assignment / security / public ABI.**
- **U3↔U4 production-private boundary candidate**（[docs/23](docs/23-usb-radio-boundary.md) §4.3.1 / §10.1 U3）: logical epoch claim（nonzero monotonic ID + bound generation）、raw outstanding 最大1の tracked TX token、C1 write の exact terminal resolution（pending / cancelled / fenced-unaccepted / raw-accepted-current / raw-accepted-then-fenced / indeterminate-partial）、accept 前 intent/wire cancel、claim 中の RX-only cold（parser+ingress のみ reset、BOUND/gen/TX/token 維持）を `src/transport/control_session.{h,c}` に追加。full accept のみ current commit、post-I/O generation raceは accept 事実保持後に fence、partial/over-report/non-OK accepted>0 は推測せず fail-closed。closed tagged RX-cold reason、epoch/token exhaustion、terminal の rebind 跨ぎ保持、same-token WOULD_BLOCK→full accept exactly once を host fake-stream で検証。gate self-test **15 mutations**、tests-OFF production archive への3 test seam symbol漏洩0、heap/VLA なし、public ABI 非変更。**NCL1 pure codec との接続境界までの候補であり、HELLO session engine / U4 complete / USB HIL / SX1262 を完成主張しない。**
- **U4 NCL1 pure codec + vector slice (codec/wire only)**（[docs/23](docs/23-usb-radio-boundary.md) §7 / §8.1–§8.6 / §8.9 IDs）: production-private `src/model/ncl1_codec.{h,c}`（exact 26-byte header, max body 998, closed catalog/binding, §8.1 steps 2–5 pure decode without reserved reject, step7 `check_reserved`, HELLO U4 exact bootstrap min=max=1 flags_supported=0, CTRL_ERROR 0x0001..0x0007, encode via scratch then copy）. Authoritative `spec/vectors/ncl1-u4-v1.json` + `tools/ncl1_u4_vector_gen.py`（vector_count **47** actual rows, required_ids union **46**, rebuild-identical check, NCG CRC re-verify, mutation self-test does not swallow failures）. Host Required: `ncl1_codec`, `ncl1_u4_vector_oracle`, `ncl1_u4_vector_gen_self_test`, `ncl1_vector_oracle_bridge`, `ncl1_u4_vector_fixture_freshness`. **Does not implement U4 logical session/LC over U3 boundary, HELLO state machine, liveness, or USB series complete. Does not claim HIL / assignment / security / public ABI.**
- **U3 C3 control-session + C4 pump implementation candidate**（[docs/23](docs/23-usb-radio-boundary.md) §4.1–4.5 / §10.1 U3; [docs/19](docs/19-m3-control-byte-stream-framing.md)）: production-private `src/transport/control_session.{h,c}` in `ninlil_runtime_private`（ESP-IDF source authority 共有）。NCG1→C1; copy-out RX; TX C1 **all-or-none**（WOULD_BLOCK full-frame residual; partial-OK / WOULD_BLOCK accepted!=0 / read OK n>cap / read WOULD_BLOCK n!=0 / feed residual ⇒ fail-closed）; ingress/TX **16/8192**; post-I/O **g1-link-g2** generation ticket; framing stats committed+live nondecreasing; WRONG_OWNER zero-mutation; **test seam** only under `NINLIL_BUILD_TESTS` + `NINLIL_CTRL_SESSION_ENABLE_TEST_SEAM`（tests-OFF/ESP production archive に seam 無し — gate 証明）; heap/VLA 禁止; public ABI 非変更。host CTest `control_session_u3` + gate（15 mutations）。**Does not implement HELLO session engine/cookie/custody/assignment/security。NCL1 pure codec は別 U4 slice。Does not claim U3 complete / U1–U2 Required HIL / USB series complete / device framing HIL / M3 complete.**
- **U2 A2 ESP32-S3 CDC-ACM implementation candidate**（[docs/23](docs/23-usb-radio-boundary.md) §3.1 / §3.4 / §10.1）: private C1 generalization（`endpoint_token` + `LINK_LISTENING`; generation advances only on physical UP; `generation_rx_discard_bytes` + `tx_driver_stale_accepted`）+ A2 under `ports/esp-idf`（fixed 4096 rings; attach+DTR → UP; **persistent USB service** ABSENT→STARTING→**READY**|POISONED; **logical bind** FREE→CLAIMING→BOUND→PARKING→FREE|POISONED; V1 close = **logical park** only — **no** `tinyusb_cdcacm_deinit` / `tinyusb_driver_uninstall` on success park; **s_io** serializes software FIFO; **flush-before-publish** + **s_live success-bind only**; close **fence any non-OK fail-closed** then unpublish→inflight drain→s_io quiesce; lifecycle mutex for open/close; **callback try_enter + epoch/inflight**; **TX drain ticket**（s_mux peek → s_io queue → s_mux commit）; WRONG_OWNER before out_* mutation; sticky core `last_error` under s_mux; WRITABLE only when UP; one-shot overflow/link events; poll **call-entry absolute deadline** + `ceil(timeout_ms×configTICK_RATE_HZ/1000)` + recheck before extra I/O; software FIFO clear only（non-recallable）; modern `tinyusb_cdc_acm.h`; no control-CDC console / no `link_anchor`）。**`esp_tinyusb==2.1.1`** structural pin + committed locks。host pure + orch CTest + `esp_usb_cdc_u2_gate`。official Docker `espressif/idf:v5.5.3` esp32s3 smoke/hil **compile/link**。**Does not claim U2 complete / Required HIL / USB series complete / U3 / SX1262。compile ≠ HIL。**
- **U1 implementation candidate / host tests**（[docs/23](docs/23-usb-radio-boundary.md) §3.2 / §4 / §10.1）: portable private C1 byte-stream contract（`src/transport/byte_stream.h`; no termios/fd/pthread types; vtable open/read/write/close/poll + link/generation/WOULD_BLOCK/RX overflow continuity; saturating stats helpers; **single-owner after open including observers**）+ A1 POSIX adapter（`ports/posix/usb_serial`; Linux/macOS; absolute path only; raw termios 115200 8N1 + poll; **`O_RDWR|O_NOCTTY|O_NONBLOCK|O_CLOEXEC`**（atomic close-on-exec; no-O_CLOEXEC 時は fcntl fallback fail-closed）; `TIOCEXCL` where supported; default DTR assert with structured unsupported evidence; fixed 4096 B RX/TX rings; all-or-none TX `WOULD_BLOCK`; owner process/thread fence; idempotent close; reopen advances nonzero link generation; poll-driven pump with EINTR/HUP/ERR/unplug-errno mapping; RX full + pending readable → continuity overflow signal **not** physical link down; structured error stage/errno/path/hint; measurement snapshot）。host CTest `posix_usb_serial_u1`（PTY + syscall seam）+ `byte_stream_portability_gate`。**Not installed / not public ABI。Does not claim U1 complete / Required HIL Linux+macOS physical USB CDC / USB series complete / ESP CDC A2 / NCG1 session / SX1262.**
- **U0 USB / physical radio boundary freeze（docs only）**（[ADR-0003](docs/adr/0003-radio-usb-dependency-direction.md) Accepted、[docs/23](docs/23-usb-radio-boundary.md)）: compile/source と runtime flow を分離; physical TX 順序は immutable wire plan → Compliance Permit(exact; SiteAssignment bind) → sole HAL transmit-with-permit → SX1262; private session object + payload ownership; RX overflow 時 parser/session fence; queue entry+byte（profile default）; Controller-only HELLO; **NCL1 header `session_cookie`（offset 16 u64; HEADER=26 / MAX_BODY=998）を全 active message で wire 検証**（HELLO_ACK body に cookie 重複なし; body exact 8）; cookie は Cell **CSPRNG/approved random 必須・取得不能なら HELLO_OK 禁止**（durable counter 代替は U4 非採用）; **NCG1 sequence（docs/19 offset 12）の USB U4 exact policy**（stream_or_cell_id=0 / +1 / gap→session fence / wrap fail-closed; **同一 process HELLO retry は `next_tx_seq`（cold しない）**; Controller restart 等 seq0 dup/regress は valid HELLO 限定 **`BOOTSTRAP_EPOCH_RESTART`**; RESET_SESSION は sequence 無条件 0 にしない）; NCL1 envelope version と control protocol version の domain 分離; PING opaque echo token + **`ping_dispatch_slack` MUST accept**（wall clock 廃止）; CTRL_ERROR loop 閉鎖; POSIX UX（termios/DTR/`cu.*`/udev 診断）; USB series Required HIL; R9≥R4+R5+R7; Network Attachment/Join umbrella は Membership/Attachment/Control HELLO 併記・relay・multi-parent は後続未確定; Owner Task Join ≠ Network Join; wire **unallocated**; `radio_usb_boundary_docs_gate`（section/table-row scoped + **63 mutation self-test**）。**USB/SX1262 production code 0。public ABI 変更なし。compile ≠ HIL。完成/実装済みの虚偽 claim 禁止。**
- **M3-slice FreeRTOS owner-task / Cell Agent / loopback**（[docs/22](docs/22-m3-owner-cell-agent-skeleton.md); **experimental**）: lifecycle authority + **inflight admission**（UAF 封じ）、serial claim、start_gate barrier、JOIN_ACK after `eSuspended` + `vTaskDelete` + SMP idle wait、**FAILED_LIVE vs FAILED_JOINED**、task/`post_tick_from_isr` 分離 + ISR critical、`StackType_t` stack depth **4096 bytes** + HWM **bytes**、backend **no shadow mailbox**、tx_gate idle atomic swap、loopback `logical_bytes==0` deny + shutdown-with-live fail。host pure authority tests。smoke dual-core + CRITICAL_COMPLIANCE self-test source。public ABI 非変更。**compile≠HIL; M3 incomplete.**
- **M3 ESP-IDF durable storage candidate**（[docs/21](docs/21-m3-esp-idf-durable-storage.md)）: format 4 dual-slot data + durable namespace directory、ESP-IDF wear levelling、PSRAM-bounded atomic final-view rebuild（binder-owned workspace / unbind責任 / 二重bind禁止）、iterator deep snapshot、provider-neutral final-net conformance、HIL observer を private header へ分離、wear `planned_full_commits_per_day` を「この物理WL partition/wear-budget domainで実際に発生する全instance/全namespaceのFULL commit合計」（別partitionは別budget・同一traffic二重加算禁止）として明記。ESP `FULL`は実機power-cut HIL完了まで`COMMIT_UNKNOWN`であり、M3/field-readyを主張しない。

- **M3-slice control byte-stream framing**（[docs/19-m3-control-byte-stream-framing.md](docs/19-m3-control-byte-stream-framing.md)）: Controller↔Cell Agent 向け transport-agnostic **`NCG1`** bounded frame codec（**production-candidate** portable implementation; production-private header / non-public wire）。magic/version/type/flags/stream_or_cell_id/sequence/payload_length/header CRC32C/frame CRC32C、max payload 1024、parser object ceiling 1152 + caller payload 1024（working set 2176）、zero heap/VLA、C11 big-endian deterministic encode、incremental parser（untrusted 1-byte+compact resync / trusted whole-candidate discard on frame CRC fail / feed guard partial re-feed）。`resync_skips` は discard **bytes**（saturate）。host golden + mutation + exact counter tests、`spec/vectors/control-frame-v1.json` + independent oracle。`ninlil_runtime_private` source authority 経由で ESP-IDF component compile/link。**public ABI 変更なし。Does not claim M3 complete / USB-CDC/TCP driver / Cell Agent / custody=receipt / security / radio wire replacement / V1 complete.**
- **M3-basic ESP-IDF platform adapters**（[docs/20](docs/20-m3-basic-esp-idf-platform-adapters.md)）: port-owned factories（public ABI 非変更）。clock staged short-config ASan-safe + alias-safe、re-init failure は side-effect-free、retained ops は shutdown 後 fail-closed。entropy: `BOOTLOADER_RNG` + **process singleton exclusive ownership**（live re-init reject without destroy、non-owner shutdown no-op、owner-only disable; RF/ADC 前 shutdown）。execution ISR→0。host lifecycle/negatives; CI target smoke は **compile/link only**（実行 HIL 非claim）。authority `cmake/ninlil_esp_idf_port_sources.cmake`。**Does not claim M3 complete / NVS / owner-task body / HIL / hardware verified / V1 complete.**
- **M3-prep ESP-IDF component packaging**（[docs/18-m3-prep-esp-idf-component.md](docs/18-m3-prep-esp-idf-component.md)、[ports/esp-idf](ports/esp-idf/README.md)）: portable Core/private sources を `ports/esp-idf/components/ninlil` として package。host と共有する単一 source authority `cmake/ninlil_runtime_private_sources.cmake`。ESP-IDF pin **`v5.5.3`**（`ports/esp-idf/ESP_IDF_VERSION`、公式 Docker `espressif/idf:v5.5.3`、host CI から分離した `.github/workflows/esp-idf.yml` で **esp32s3** smoke build）。packaging gate `tools/esp_idf_component_packaging_gate.py`。**Does not claim M3 complete / ESP-IDF port complete / NVS / FreeRTOS owner task / USB / Wi-Fi / SX1262 / hardware verified / V1 complete.**
- Domain Store **D3-S2a Normative freeze**（docs only: [docs/17 §18.13](docs/17-foundation-domain-store.md); from three adjudications + post-write audit correction）。snapshot-only declared multi-count scope; closed modes **21..26** / **k₂=6**; **1 session = 1 mode = 1 READ_ONLY txn**（D3-S2 complete = **six** self-contained sessions; mode-scoped COMPLETE/cost/oracle）; actual **SHA256_COMPOSITE** keys are **not owner-contiguous**（§18.5 category-B applicability corrected without rewriting S0 history: S2 uses category **D** focus multipass）。same-txn phase machine: baseline D2 once then sequential close/reopen of only zero-prefix iterator（max one live iterator）; **PASS_INTERNAL** freezes D2 counters/reconciliation **and** future/profile diagnostics, skips recognizable future rows for S2, resets only pass-local lex; **B0–B11** row_budget/drive resume rules; mode-dependent carrier sets + empty-carrier still runs BIND then COMPLETE; focus count + known-slot + stream split; Mode21 FOCUS/BIND ATTEMPT+INDEX; Mode22 FOCUS_ATTEMPT + BIND_ATTEMPT only（INDEX absence via BIND_ATTEMPT ABSENT peer; no FOCUS_INDEX/BIND_INDEX）; **CLEANUP_PLAN** PRESENT skips ordinary attempt/index count only（Mode21 index equality; Mode22 attempt ordinary）and defers remaining to S11; BIND entry requires `focus_live==0` and reuses `focus_raw80`/`focus_primary_key_digest`/`source_aux` as per-row scratch; **P0 BIND carrier companion**: every BIND secondary proves same-txn declared-count carrier presence/body subject match **in addition to** true-primary PVD/raw（Mode21 ATTEMPT: TRANSACTION_STATE+ANCHOR+INDEX PRESENT; Mode21 BIND_INDEX: ANCHOR+ATTEMPT pair only, STATE covered via paired ATTEMPT walk; Mode22 ATTEMPT: RESULT_CACHE+DELIVERY+INDEX ABSENT; Mode23: matching STATE|RESULT+primary; Mode24: RESULT+DELIVERY; Mode25 RECENT: same-tx CUM+ANCHOR, CUM self-carrier, RECENT-without-CUM corrupt; Mode26: EVENT_SPOOL+ANCHOR）; carrier ABSENT/wrong subject = real S2 orphan finding, Port fail remains no-note; empty-carrier+non-empty secondary fails through BIND; get budget freeze ≤1 primary + ≤1 carrier companion + ≤1 pair peer（max 3 ATTEMPT）; generic declared/observed **A/B/C** u64 lanes with exact mode map（Mode21 app/cancel/INDEX=A+B; Mode22 app/cancel/MBZ; Mode23 valid/overflow/late vs M/MBZ/late_material with `A=obsA+B` and `obsC≤C≤A` without false late equality; Mode24 reply popcount; Mode25 cycles/min4/CUM; Mode26 management; unused MBZ）; trailing controls **`binding_complete_mask`/`focus_mode`/`cleanup_skip`** with exact mask bits; honest **O(Fₘ·N + mode BIND walks)** per session / sum×6 complete; fixed RAM / no heap / VLA / full-ID; exact all-u8 context layout **sizeof 306 / align 1 / ceiling 320** with published offsets and outer future aggregate ceiling **9152** while scanner **8192** / Stage5-alone **8704** / S1 sizes unchanged; S1/S2 contexts mutually exclusive per scanner session（DSD1 multi-session orchestration, not dual-bound）; private API/seams contract names only; append-only d3s2 oracle architecture retaining exact **94-vector d3s1 prefix** and frozen upstream pins; exhaustive mode-scoped case classes/anti-false-pass; **DSD1 composition not new mode**; explicit exclusions and nonclaims。**JSON / code / tests / CMake / tools / vector / ADR 変更 0**。historical D3-S0 / D3-S1a text preserved。**D3-S2a docs freeze only complete; D3-S2 implementation / D3 overall / Stage 5 still pending.**

- Domain Store **D3-S1 implementation complete**（[docs/17 §18.2 / §18.7 / §18.12](docs/17-foundation-domain-store.md); separate from historical **D3-S1a docs-only** freeze）: private fixed D3 context (`sizeof` 421 / align 1 / ceiling 448) + modes 1..20 enum; pure peer-key rebuild for **Modes 1–16 forward** + **Mode 17 REV_PRIMARY** closed reverse table + **Modes 18–20** local gates; private `ninlil_domain_scan_begin_profiled_d3s1` prevalidation (mode ∈ 1..20 valid; mode 0/>20 INVALID_ARGUMENT Port 0; no UNSUPPORTED poison for closed modes); session non-owning `bound_d3_context` (D2 `begin_profiled` remains D3-inactive); per-row evaluator Modes 1–20 after S3 / before `previous_key`+`ok_row_count` (Port terminal = no note; Mode17 source header PVD vs primary VALUE_DIGEST; Mode19 never compares reservation PVD to RESULT; Mode20 §9 terminal/active matrix). Sibling oracle `spec/vectors/domain-scan-crossrow-v1.json` format **`ninlil-domain-scan-crossrow-v1-d3s1`**（`vector_count` **94** / full JSON sha256 `f47dff4f5753a92ebf3627408c576f69cc1862d20e1f74021e22ef5603c87176` / canonical `content_sha256` `76b28d847be8cd7a95e8f1879400403abf702931a3de170a473c7c0f76d95468`）+ independent generator `tools/domain_scan_crossrow_vector_gen.py` + production bridge + exact 94 kinds + mutation 0 + frozen D1/S1–S5 pins. DSR2/`-Wvla` covers `domain_store_d3s1.c`. **Does not claim D3-S2..S12 / D3 complete / Stage5 D3 bind / Stage 5 / D4 / public Runtime / ESP-IDF / hardware / SDK-field-V1 readiness.**
- Domain Store **D3-S1a Normative freeze**（docs only: [docs/17 §18.12](docs/17-foundation-domain-store.md)）。relationship-focused exact-1 design（**not** packed multi-peer）: one closed mode per fresh profiled session; per applicable row ≤1 peer-key rebuild + ≤1 `exact_get`; fixed **k=20**; honest **O(20·N)**; mode mismatch skips get but mode itself is mandatory。closed modes 1–16 forward（SERVICE→QUOTA/RES; TX→SEQ/STATE/IDEM/RES/SCHED + family-gated EVENT_MAP/EVENT_SPOOL/CANCEL; DELIVERY→RESULT/RES/SCHED + family-gated CANCEL; ORDERED_INGRESS→RES; ORDERED_INGRESS→SCHED with Mode16 correction: NEW_DELIVERY and EXISTING_* scheduler **PRESENT**, never ABSENT; transfer/cardinality remains S2）、Mode17 closed enumerated REV_PRIMARY true-primary reverse table（SERVICE_QUOTA→SERVICE; RES owner SERVICE/TX/INGRESS/DELIVERY→that owner primary; **CALLBACK RES→DELIVERY** not RESULT, PVD is DELIVERY; SEQ/STATE/IDEM/EVENT_MAP/EVENT_SPOOL/RETRY/MANAGEMENT→ANCHOR; SCHEDULER→subject primary TX/DELIVERY/INGRESS; ATTEMPT→owner TX/DELIVERY; ATTEMPT_ID_INDEX→ANCHOR for live PVD only; CANCEL/EVIDENCE→owner TX/DELIVERY; RESULT/REVERSE_REPLY→DELIVERY; RETENTION_BASIS/CLEANUP_PLAN→subject TX/DELIVERY reverse PVD/raw only with CLEANUP phase/cardinality S11; every reverse edge PRESENT true primary + live VALUE_DIGEST vs source header PVD + byte raw identity; IDEM reverse uses both dual-raw buffers; exclude BLOB→S3, witness exceptions, family5 primary markers）、Mode18 ATTEMPT↔ATTEMPT_ID_INDEX local gate only（no counts）、Mode19 RESULT_CACHE→CALLBACK RES ACTIVE gate（presence + delivery raw + token_generation only; **do not** compare reservation header PVD to RESULT value; Mode17 independently proves CALLBACK RES PVD against DELIVERY）、Mode20 GATE_RETENTION_BASIS（TRANSACTION_STATE or RESULT_CACHE lifecycle row derives subject retention key; terminal retained PRESENT / active ABSENT via existing §9 terminal/nonterminal matrix without inventing enum cases; presence+subject raw only; peer retention PVD independently Mode17; cleanup phase/cardinality S11）。exclusions: S2 multi-count、S3 BLOB、S9 quota aggregate、S11 cleanup phase/cardinality、KEY_DIGEST reverse。multi-peer packing rejected（exact_get destroys source value/typed; DesiredState two absent edges; raw collision defense including dual-RAW16）。fixed context layout all-uint8: `expected_pvd[32]` `peer_key[45]` `source_raw[255]` `source_raw2[64]` `source_aux[16]` + **nine** control bytes (`peer_key_len` `source_raw_len` `source_raw2_len` `source_aux_len` `mode` `flags` `source_subtype` `expect_presence` `owner_kind`) = **sizeof 421 / align1 / ceiling 448**。**dual RAW16 Mode5/IDEM reverse:** scope≤255 + idempotency key≤64 byte-exact; no hash substitute。memory: current scanner 8096≤8192、session 136、Stage5 8376≤8704; future session **144** / Stage5 future **8384** unchanged; aggregate packed sum **8805**（8384+421）、outer align8 actual **8808**; **NEW D3 aggregate arena ceiling 8832**（= 8384+448）; scanner **8192** and Stage5-seam-alone **8704** unchanged; explicit doc-first **+128** over Stage5-alone 8704; Stage5 does not allocate/bind/run D3 until S12。private API contract: existing `begin_profiled` remains D2-only; future `begin_profiled_d3s1` requires mode **1..20** + non-null disjoint context at prevalidation; Stage5 continues old begin until S12; no nullable optional skip。evaluator after S3 / before previous_key ok_count; Port terminal = D2 sticky/no note; missing/present/PVD/raw/dual-raw mismatch + Mode16/19/20 gates = real D3 findings + note; workspace key preserved; source raw copy-before-get。oracle architecture only（all **20** modes; two fresh sessions for DS EVENT_MAP/SPOOL absent; restart/front-key; null/bad-mode 0/>20/alias; Port-no-note; raw collision; CALLBACK split Mode19/17; profile skip; D2 begin no-D3）— **JSON not created**。truthful dual-raw field audit replaces false single-raw “pending modes 0” claim。**D3-S1 implementation / D3 complete / Stage 5 / D4 / public Runtime still pending.** No code/test/CMake/tools/vector JSON/ADR changes; does not claim implemented APIs/types/constants exist。
- Domain Store **D3-S0 Normative architecture freeze**（docs only: [docs/17 §18](docs/17-foundation-domain-store.md)）。ownership close（D1 grammar + existing `build_key`/`key_digest`; typed peer rebuild = D3 work items; D2 traversal/structural/exact_get/note mechanism; D3 Stage5 steps 5–10 cross-row finding correctness with step8 map: ingress/attempt/delivery/result/event → S1/S2, SERVICE_QUOTA → S9; D4 mutation/convergence）、ordered slice ledger D3-S0..S12（implementation dependency, not Stage5 scan order）、hybrid evaluator（per-row after S3 + fresh multi-pass; no second concurrent iterator; no full-ID set; no D4 writes）、descriptor lifetime/copy-before-exact_get（D3 fixed context size TBD at S1 under Stage5 8704 / DSR2; no guessed size; **superseded for size by D3-S1a §18.12**）、algorithm categories + honest fixed-`k` vs data-dependent `F`/`S` costs（SERVICE_QUOTA O(S·N) profile-bounded; may be quadratic-class wall time when F scales with N; fixed memory; ban undocumented/unbounded all-pairs and false O(N); do not ban documented profile-bounded multipass）、invariant ownership table（DSI1 / DSW1–3 / DSC1–2 / DSH1 + DSC3/DSD1/DSH2/DSO1–2 shared）、sibling oracle schema ownership without adding JSON（D1/S1–S5 read-only pins; crossrow append-only d3sN; pin update procedure without claiming file exists）、corruption reporting（real D3 finding only → `note_terminal_corrupt`）、Stage5 outcome ladder（retain private `EXISTING_SCAN_ADOPTED_D3_PENDING` until S12 → private `D4_PENDING`, name frozen at S12; `storage_recovery_complete=0`）、missing helper needs without claiming code。**D3 implementation / Stage 5 / D4 / public Runtime still pending.** No code/test/CMake/vector JSON changes; no new ADR。
- D2-S6 private fail-closed Stage 5 seam implemented（S6a L2b1 BUFFER_TOO_SMALL→CORRUPT no-reread/no-alloc including key>255 and value 4097/65536, value workspace 4096, allocator removed from private L2b1 API; S6b `runtime_store_stage5_seam.{c,h}` in `ninlil_runtime_private` only: L2b1 first → `NEW_BOOTSTRAP_STAGE5_PENDING` or `EXISTING_SCAN_ADOPTED_D3_PENDING`; begin_profiled + advance(64) + finalize; storage_recovery_complete remains false; prevalidation poison-retention before out_result/workspace mutation; never OK+OUTCOME_NONE; `scan_ran` only after scanner lifecycle entry; `tools/runtime_store_stage5_gate.py` + negative self-tests）。**Stage 5 / D3 / D4 / public Runtime still pending.** Does not claim D3/D4/identity/health/runtime_create/Bearer/clock/entropy/Stage9/ESP-IDF/hardware。
- Domain Store D2-S5 bounded scanner composition（`DSR1_SCAN` complete + `DSR2_ESP_BOUND` complete + **D2 bounded scanner complete**）。production-private `ninlil_domain_scan_note_terminal_corrupt(session)`（NULL→INVALID_ARGUMENT; legal OPEN/EXHAUSTED only: Port 0, first sticky STORAGE_CORRUPT, state FAILED, preserve flags/counters/previous/workspace/iter-txn; IDLE/DONE/FAILED→INVALID_STATE no mutation; no status arg; no public include）。D3 finding correctness remains D3; S5 supplies injection/aggregation only (note then advance/exact_get reject; finalize/abort cleanup; corrupt outranks future/profile candidates)。sibling oracle `spec/vectors/domain-scan-composition-v1.json`（format `ninlil-domain-scan-composition-v1-d2s5`; 22 vectors / 21 required kinds including `note_exhausted`）、independent generator `tools/domain_scan_composition_vector_gen.py` with per-call `expected_status`（scanner status or harness `VOID`）、closed call-op set including `handle_drift`、restart budget1 + DONE-before-`session_init`、exact kind-set and anti-false-pass checks、full S1/S2/S3/S4/D1 SHA+format+count pins and full deterministic document equality、production bridge（per-call status、exact_get counter snapshots、full Port-trace equality without dropped enums、all-kinds bitmask）、focused units including first-sticky preservation invariant-only。profiled budget 1/64、same-snapshot exact_get lifecycle、fresh-session restart from front (not same-session budget resume)、changed snapshot restart asserts new front key、FAILED cleanup restart、rollback failure sticky、unknown rollback fence-once、handle drift closes original、future/mismatch then note→corrupt、state gates、close/fence once、mutation0。DSR2 complete: current Normative ceiling 8192 (not forever)、live sizeof、single 4096 value buffer、no second 4096/full-ID/xref fields、`tools/domain_scan_dsr2_gate.py` complete source gate (comment/string strip、allocator/65536/re-read bans、call-graph no direct/mutual recursion、auto workspace/record ban、fail-closed parse) + negative self-tests + compiler `-Wvla` on `domain_store_scanner.c` under `-Werror` (C11 extensions OFF; no permanent stack-byte ceiling claim); tests-OFF private symbols include note and exclude transport begin。S1–S4 and D1 JSON byte-for-byte frozen。**Does not claim Stage 5 / D3 finding correctness / D4 mutation / S6 / public Runtime / ESP-IDF compile / hardware**。S0 historical freeze remains “did not complete D2 at freeze time”; current S5 completion is unambiguous without rewriting S0 history。
- Domain Store D2-S4 same-snapshot production-private exact `get` seam（`ninlil_domain_scan_exact_get` legal in `OPEN`/`EXHAUSTED` only; sole live zero-prefix iterator; exact one Storage `get` on bound READ_ONLY txn; does not consume `row_budget` or change ok-row counters/iterator position）。private presence enum `ABSENT`/`PRESENT` + borrowed `workspace->value` observation（present zero-length via presence; clean NOT_FOUND → OK+ABSENT）。Port-path failure leaves caller output unchanged and sets sticky/`FAILED` as `advance`。S4 key alias exception: key may borrow external or `key[]`/`previous_key[]` but must be disjoint from `value[]`。No unused xref digest/kind/count session fields; no second 4096 buffer; workspace `sizeof <= 8192`; mutation 0。sibling oracle `spec/vectors/domain-scan-exact-get-v1.json`（format `ninlil-domain-scan-exact-get-v1-d2s4`）、independent generator with S1/S2/S3 full SHA pins、production bridge、unit acceptance。S1/S2/S3 JSON and D1 d1b3o JSON byte-for-byte frozen。**D2-S4 implementation complete only** — does not claim D2 / DSR1 / DSR2 / Stage 5 / public Runtime / D3 relationship semantics / full-ID set / ESP hardware。
- Domain Store D2-S3 exact-profile structural same-record scan path（family 5/6 CURRENT closed catalog family5 `01`+family6 §7 全29 subtypes REQUIRED → `ninlil_model_domain_validate_typed_record` for business+`7d` with public API large-local separated into no-output helper; `7e`/`7f` scanner-local parse key+envelope+pure decode+key/body/header bijection + independent header mutates）。workspace `row_validate_scratch` union（typed/witness; `sizeof <= 8192`）。status: `UNSUPPORTED`（record_version/domain_format future）non-terminal with true future→corrupt precedence; profile mismatch / future_profile skip S3 decode; BTS 4097 / unknown subtype / lex OOO。sibling oracle `spec/vectors/domain-scan-structural-v1.json`（format `ninlil-domain-scan-structural-v1-d2s3`）、independent generator with D1 d1b3o full SHA/count pin composition + S1 transport body-nonvalidation hash/ID pin、production bridge、closed catalog coverage。S1/S2 JSON and D1 d1b3o JSON byte-for-byte frozen。**D2-S3 implementation complete only** — does not claim D2 / DSR1 / DSR2 / Stage 5 / public Runtime / S4 exact-get / D3 cross-row / ESP hardware。
- Domain Store D1-B3o CLEANUP_PLAN (`0x63`) pure body encode/decode、126+N body same-record validator（family6 flags0 / COMPOSITE(63, subject_kind||subject_primary_key_digest) / primary_id TX raw16 or DELIVERY composite first16 / PVD+head nonzero / record_revision==batch_generation>=1 / subject_primary_key_digest=KEY_DIGEST complete primary not bare composite / subject_primary_value_digest NZ + equals header PVD / cleanup+batch generation>=1 / count inequalities + phase1/2/3 remaining/fence closed matrix）、independent oracle format `ninlil-domain-store-v1-d1b3o`（append-only after 1460 B3n vectors; prior prefix byte-for-byte）、parser bit44 (synthetic future test bit→45) / tests。Does not implement D2-S3、Stage 5、or public Runtime; live counts/basis/fence aggregate are D3。
- Domain Store D1-B3n RETENTION_BASIS (`0x61`) pure body encode/decode、90+N body same-record validator（family6 flags0 / COMPOSITE(61, subject_kind||RAW16) / primary_id TX raw16 or DELIVERY composite first16 / PVD+head nonzero / revision>=1 / subject_primary_key_digest=KEY_DIGEST complete primary not bare composite / ACTIVE pending|overflow|trusted + ELIGIBLE + CLEANUP_COMMITTED closed matrix / bools 0|1 / checked basis+window）、independent oracle format `ninlil-domain-store-v1-d1b3n`（append-only after 1391 B3m vectors; prior prefix byte-for-byte）、parser bit43 / tests。Does not implement D2-S3、Stage 5、or public Runtime.
- Domain Store D1-B3m MANAGEMENT_LEDGER (`0x52`) pure body encode/decode、exact 364-byte same-record validator（family6 flags0 / composite key=tx||op plain / primary_id=tx / PVD+head nonzero / immutable revision exact1 / kind15 EVENT_RESUME + kind16 EVENT_DISCARD closed matrix / independent streaming SHA-256 canonical request digest recompute）、independent oracle format `ninlil-domain-store-v1-d1b3m`（append-only after 1287 B3l vectors; prior prefix byte-for-byte）、parser bit42 / tests。Does not implement B3n–B3o、D2-S3、Stage 5、or public Runtime.
- Domain Store D1-B3l RETRY_SUMMARY (`0x51`) pure body encode/decode、CUMULATIVE exact 84 / RECENT exact 80 same-record validator（family6 flags0 / composite key=tx||kind||slot / primary_id=tx / PVD+head nonzero / revision>=1 / kind-slot-fold arithmetic / bools / empty-aggregate when folded=0）、independent oracle format `ninlil-domain-store-v1-d1b3l`（append-only after 1222 B3k vectors; prior prefix byte-for-byte）、parser bit41 / tests。Does not implement B3m–B3o、D2-S3、Stage 5、or public Runtime.
- Domain Store D1-B3k EVENT_SPOOL (`0x50`) pure body encode/decode、exact 300-byte same-record validator（family6 flags0 / ID128=tx / primary_id=tx / PVD+head nonzero / record_revision==spool_revision / state×cause / resume 0..8 / discard iff DISCARDED / reservation KEY_DIGEST）、independent oracle format `ninlil-domain-store-v1-d1b3k`、parser/bridge/tests。Does not implement B3l–B3o、D2-S3、Stage 5、or public Runtime.
- Domain Store D2-S2 production profiled begin（same-txn 17 exact get + profile gate + one-iterator reconciliation）、sibling oracle `spec/vectors/domain-scan-profile-v1.json`（format `ninlil-domain-scan-profile-v1-d2s2`）、independent generator、production bridge、and unit acceptance。S1 transport begin is TEST-macro only and absent from tests-OFF private symbols. Does not complete D2 / DSR1 / DSR2 / Stage 5 / hardware.
- Foundation M1aのpublic C ABI headerとC11/C++17 consumer smoke。
- ABI manifest、reason registry、Operator projection、hook registry、仕様vector、requirements traceabilityの検査tool。
- Scheduler、deadline、Required Receipt、resource accounting、Submission preflight/admissionのpure C11 model。
- Runtime config/Platform検証、11種capacity導出、Storage/Bearer/Clock/entropy分類、Stage 9 health gateを含むRuntime Lifecycle L1 pure C11 model。
- Runtime Store v1の17 bootstrap key、typed big-endian record、CRC32C、境界/破損検査を行うportable C11 codec。
- Stage1 successだけが発行するheader/pointer-free accepted-config projectionに束縛したcanonical profile/identity、17-record aggregate validation、presence/profile/identity decision、compact lazy bootstrap planのRuntime Store L2a2 pure C11 model。
- Lifecycle/Runtime Store sourceをTEST-only reducerから分離した、非export・subproject build対応の`ninlil_runtime_private` STATIC targetとcomposition smoke gate。
- Atomic FULL admission write-setとcommit result別のownership/recovery model。
- In-memory Storage、Allocator、Execution、Virtual Clock、Deterministic Entropy v1のTEST conformance fixture。
- 2 endpoint typed simulated Bearer、bounded FIFO、receive loan、shared Virtual Tx Gate / one-shot permitのTEST conformance fixture。
- Stateless synthetic grant、closed deny precedence、fault seam、TEST-only composition assertionを備えたOrigin Authorization fixture。
- Ubuntu GCC通常buildとClang ASan/UBSan buildを実行するGitHub Actions CI。
- Apache License 2.0、contribution guide、direct confidential reporting linkを含むsecurity policy。

### Changed

- **U2 A2 close/rebind/unbound RX I/O serialization（P0）+ poll deadline / s_mux depth-0 gate（P1）:** firmware-lifetime **`s_io` mutex** で TinyUSB software FIFO 直列化; **flush-before-publish**; close **unpublish-first**; RX **s_io first**。P1: gate が **portENTER_CRITICAL 区間内の real driver/I-O 呼出しを構造スキャンで拒否**（marker 残し注入 mutation）; poll は **call-entry tick + `poll_required_ticks_hz` + delay 後 I/O 前 recheck**（C1 整合）。pure bind/I-O + poll deadline tests。**Does not claim U2 complete / Required HIL / wire recall.**
- **Host CMake generated-fixture race fix（build reliability; U2 非依存）:** 同一 build 内で `domain_scan_crossrow_vector_fixture.h` を 2 executable の `SOURCES` に直付けしていたため、**Make/Ninja 等 parallel generator** が同一 `add_custom_command` OUTPUT を二重起動し、途中書込み header を compile が読む race（`ninlil_d3s1_*` unknown / cascading error）を起こし得た。正攻法: **1 OUTPUT + dedicated `add_custom_target` + 両 bridge は header を SOURCES に載せない / `add_dependencies` + `OBJECT_DEPENDS` + binary include dir**; generator は temp+atomic replace。構造 gate `tools/cmake_generated_fixture_source_gate.py`。**Does not claim U2 complete / Required HIL / USB production change.**
- **Installed CMake package imported-config mapping:** single-config `install(EXPORT)` が producer の1 configuration（`DEBUG`/`RELEASE`/`NOCONFIG`）分の `IMPORTED_LOCATION_<CONFIG>` しか出さないため、空 `CMAKE_BUILD_TYPE`・別 build type・厳格 `CMAKE_MAP_IMPORTED_CONFIG_*` identity map の installed consumer が `IMPORTED_LOCATION not set for ... configuration "Release"` 等で generate 失敗する既知問題を修正。`NinlilConfig.cmake` が (1) `Ninlil::ninlil_posix_sqlite_storage` に config 非依存 `IMPORTED_LOCATION` と不足 config 向け `MAP_IMPORTED_CONFIG_*` を付与、(2) FindSQLite3 の config-less `IMPORTED_LOCATION` を standard config 名へ **additive 複製のみ**（`INTERFACE_*` / link line は非改変; ALIAS は触らない）。install-consumer smoke を match / no-`CMAKE_BUILD_TYPE` / Debug↔Release / strict-map（および multi-config generator 時は Debug+Release）matrix に拡張。STATIC/SHARED SQLite・archive hygiene・tests-OFF package surface・sanitizer skip 規則は維持。
- Project statusをPR1-only checkpointから、Foundation PR2主要modelとPR3a/b/c/d fixture実装済みのpre-alpha checkpointへ更新。
- Apache License 2.0の採用決定をProject CharterとM11 compliance gateへ反映。
- `1.0.0`はhardware exitだけでなくM11までの全exit gate後とし、platformの現行検証実績とplanned targetを分離。

### Known limitations

- Public Runtime APIのfunction bodyと`runtime_step`は未完成です。
- Bearer、Tx Gate、Origin Authorization、Reliable Command、Durable Eventはend-to-endで未統合です。Runtime 統合済み production durable storage も未完了です（ESP dual-slot candidate / POSIX SQLite host candidate はいずれも Runtime body 未接続; field-ready 非claim）。
- ESP-IDF port の storage FULL attestation / physical power-cut HIL、USB series 完成、Wi-Fi、SX1262 production TX、Display/Leak 実機 path は未完成です。M3-prep は component packaging、M3-basic は clock/entropy/execution adapters、control framing は private `NCG1` codec、owner/cell/loopback skeleton は [docs/22](docs/22-m3-owner-cell-agent-skeleton.md)、storage candidate は host conformance + ESP_UNPROVEN（[docs/21](docs/21-m3-esp-idf-durable-storage.md)）、U1 は host POSIX USB/serial **implementation candidate**（Required HIL pending; complete ではない）、U2 は ESP CDC-ACM **implementation candidate**（host pure + target compile/link; Required HIL pending; complete ではない）、U3 は C3/C4 framing-session + U3↔U4 boundary **implementation candidate**（host fake stream）、U4 は NCL1 pure codec + logical session host **candidate**（Required host vectors/gates; complete / series 完成 / HIL ではない）、R1 は `ninlil_radio_hal` **host candidate**（sole transmit-with-permit + spy; R2 permit authority / real SX1262 / RF HIL 未）までです。public Runtime wiring / assignment / security / complete / series 完成は未実装です。
- Public API、wire、storage formatの互換性はまだ保証しません。
- Security、regulatory compliance、production SLO、field readinessを保証するreleaseではありません。

このcheckpointはpre-alphaです。Fixtureやsimulator testの成功は、production deploymentや実radioの安全性・信頼性を証明しません。
