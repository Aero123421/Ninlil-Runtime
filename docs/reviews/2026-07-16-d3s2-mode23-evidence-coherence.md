# D3-S2 Mode23 evidence coherence review

Date: 2026-07-16  
Scope: Mode23 `BIND_EVIDENCE`, counter coherence, append-only cross-row oracle

## Outcome

GO. No P0/P1 findings remain.

Mode23 now derives slot legality per EVIDENCE row from the accepted exact
profile and that row's matching carrier body. Retained TX and delivery
APPLICATION_FIRST allow slots `0..L`; delivery CANCEL_FIRST permits exactly
zero EVIDENCE rows. The global BIND pass does not use the last FOCUS
`declared_L` scratch as authority.

Illegal slots and CANCEL_FIRST evidence stop after the carrier get and body
validation, before the true-primary get. Port failures keep the existing
no-note sticky-failure behavior.

## Regression authority

The append-only cross-row authority grows from 133 to 139 vectors while the
first 133 vector objects remain exactly unchanged. The six P1-C1 vectors cover:

- TX slot `L+1`
- Mode23 counter equation mismatch
- late-evidence coherence mismatch
- CANCEL_FIRST slot 0
- a legal TX owner when CANCEL_FIRST is the last focused carrier
- legal TX evidence followed by illegal CANCEL_FIRST evidence in BIND order

The D1-derived EVIDENCE material is generated by the pinned D3-S2 wire fixture
generator. CTest checks source pins, deterministic generation, and build
fixture freshness.

## Independent QA

- generator check: 139 vectors (`94` D3-S1 prefix + `45` D3-S2 suffix)
- generator self-test: pass in 5.85 seconds
- generated JSON vs checked-in JSON: byte-identical
- prior 133 vector objects: exact match to `origin/main`
- Debug full CTest: 86/86
- ASan/UBSan full CTest: 86/86 (macOS leak-detection option omitted because
  Apple ASan does not support it; Linux CI retains leak detection)
- tests-OFF private-symbol gate: pass through CTest
- DSR2 fixed-memory gate and self-test: pass
- `git diff --check`: pass

Independent adversarial review also temporarily modeled two rejected
implementations. A profile-only slot check fails the CANCEL_FIRST vector, and
a last-`declared_L` TX check fails the multi-owner success vector.

This slice does not claim D3-S2 or D3 completion, Stage 5 completion, public
Runtime readiness, ESP-IDF readiness, or hardware readiness.
