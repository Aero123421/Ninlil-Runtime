### P2

**1. nested 整数閉スキーマが網羅しきれていない（P2-1 実効性の残）**  
`tools/domain_scan_crossrow_d3s3_vector_gen.py:13486-13497` / walk `:13503-13515`

- `event_start=false` → 正しく exit 経路（`must be JSON integer`）
- `row={}` / `value_hex` 欠落 → 必須 string 検査 OK
- しかし `_KNOWN_JSON_INT_RANGES` に無いキーは **型検査されない**
- 実測: `session.previous_key_length=false` → `None`（通過）  
  `d3s3.last_carrier_key_len=false` → `None`（通過）

報告3ケースは塞いだが、「nested 整数閉スキーマ網羅」は未完。

**2. docs 件数/SHA/bridge 正規化が中途（P2-2 残）**

| 場所 | 状態 |
| --- | --- |
| `spec/vectors/...json` | **正** count 283/139/138, content `e2c057e8…`, raw `9d6bb6ae…` |
| `docs/17` R28 段落 / work table | おおむね R28 に更新 |
| `docs/reviews/...candidate.md:10-28` **冒頭 pin 表** | **旧** R27-Sol 280 / `93edadc3…` / bridge 未追従 |
| 同 file 年表 R28 行・下部 table | R28 283 記載あり → **文書内矛盾** |
