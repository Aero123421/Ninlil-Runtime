# V1-LAB unit 3 実行証拠（B2-LAB provider 統合）

作業dir: `/Users/dt/job/LoRa/ninlil-d3s3-implementation`  
一時build: `tmp-v1`（`-DNINLIL_ENABLE_SANITIZERS=ON`）  
前提: unit 1a/1b/1c + unit 2a/2b commit 済み

## A. POSIX 全 provider 完成昇格

正本 matrix: `docs/work/2026-07-23-v1-posix-provider-matrix.md`

| Provider | factory/owner/shutdown/restart | conformance CTest |
| --- | --- | --- |
| allocator / execution / clock / entropy | `ninlil_posix_lab_platform_*` | `v1_posix_provider_conformance` |
| storage | `ninlil_posix_sqlite_storage_*`（platform 内包） | `posix_sqlite_storage` + `v1_posix_sqlite_restart_e2e` |
| bearer / tx_gate | `typed_simulated_bearer`（platform 内包） | `v1_posix_provider_conformance` |
| origin_authorization | `canonical_origin_authorization`（platform 内包） | `v1_posix_provider_conformance` |

実装: `ports/posix/lab_platform/ninlil_posix_lab_platform.c`  
公開 factory: `ports/posix/include/ninlil_posix_lab_platform.h`

## B. Runtime body 実結線

| 経路 | 検証 |
| --- | --- |
| `runtime_create` が platform set 8 slot 必須を満たす | `v1_posix_platform_restart_e2e` create 成功 |
| `runtime_destroy` が bearer/storage を close | 同上（二重 destroy 負例 `test_double_runtime_destroy_reject`） |
| platform `destroy` が provider 逆順解放 | `v1_posix_provider_conformance` restart + NULL destroy |

## C. ESP provider matrix 実測（1c 更新）

正本: `docs/work/2026-07-23-v1-esp-provider-matrix.md`（変更なし; 実測再確認）

| Provider | Status（実測） |
| --- | --- |
| allocator / bearer / origin_authorization | LAB unavailable fail-closed |
| execution / clock / entropy / tx_gate / storage | implemented |

LAB unavailable 実証: `v1_esp_provider_availability` Passed（ops 非 NULL でも `PERMANENT_FAILURE`）

## D. restart E2E 拡張

| テスト | 内容 | 結果 |
| --- | --- | --- |
| `v1_posix_sqlite_restart_e2e`（1b） | storage provider shutdown/reopen + recovery 4 種 | Passed |
| `v1_posix_platform_restart_e2e`（3 新規） | platform 全 provider restart + runtime create→step→destroy→re-create | Passed |

## E. CTest（ASan/UBSan）

```bash
cd tmp-v1
cmake .. -DNINLIL_ENABLE_SANITIZERS=ON -DCMAKE_BUILD_TYPE=Debug
cmake --build . -j8
ctest
```

結果（2026-07-23 実行）:

```
100% tests passed, 0 tests failed out of 237
Total Test time (real) = 267.77 sec
```

unit 3 関連:

| CTest | 結果 |
| --- | --- |
| v1_posix_provider_conformance | Passed |
| v1_posix_platform_restart_e2e | Passed |
| v1_posix_sqlite_restart_e2e | Passed |
| posix_sqlite_storage | Passed |
| v1_esp_provider_availability | Passed |
| v1_runtime_spine | Passed |
| v1_runtime_delivery | Passed |

```bash
ctest -N | tail -1
# Total Tests: 237
```

## F. ESP-IDF target build

ローカル: docker daemon 未起動のため `espressif/idf:v5.5.3` target build は未実行。  
CI 正本: `.github/workflows/esp-idf.yml`  
ホスト gate: `esp_storage_map_gate` / `v1_esp_durable_success_source_gate` Passed

## G. stub/TODO 0（grep）

```bash
rg -i '\bTODO\b|\bFIXME\b|\bstub\b' \
  ports/posix/lab_platform \
  ports/posix/include/ninlil_posix_lab_platform.h \
  tests/port/v1_posix_provider_conformance_test.c \
  tests/runtime/v1_posix_platform_restart_e2e_test.c
```

結果: 0 件

RC=0
