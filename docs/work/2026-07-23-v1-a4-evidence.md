# V1-LAB unit 4 実行証拠（B3 論理 capability 層）

作業dir: `/Users/dt/job/LoRa/ninlil-d3s3-implementation`  
一時build: `tmp-v1`（`-DNINLIL_ENABLE_SANITIZERS=ON`）  
前提: unit 1a–1c + unit 2a/2b + unit 3 commit 済み（barrier trunk 1–3 完了）

## A. 実装経路

| 経路 | 実装 | allowlist |
| --- | --- | --- |
| priority / deadline / retry 本経路 | `runtime_v1_delivery_durable.c`（priority ソート dispatch、`handle_timeout_retry`、`commit_terminal_outcome`） | ops 11–13（`DELIVERY_*` / `RETRY_STATE_COMMIT`） |
| submit admission + bearer 上限 | `runtime_v1_spine_durable.c` + `runtime_v1_capability.c` | op 5 `SUBMIT_ADMISSION_COMMIT`（TX v2 value） |
| logical payload / fragment（論理のみ; wire frag は V2） | `runtime_v1_capability.c`（`ninlil_rt_v1_build_logical_*`、fragment_count=1） | — |
| reservation commit / terminal release | `runtime_v1_capability.c`（`RV` marker、`ninlil_rt_v1_release_transaction_reservation`） | kind 30 `SPINE_RESERVATION`、op 14 `RESERVATION_COMMIT` |
| restart scan（TX v2 / RV） | `runtime_v1_delivery_durable.c` `delivery_restart_scan_pass` | kinds 21, 30 |
| bearer 上限表（P0-4） | `g_bearer_limit_table[]`（SIMULATED/U6 = 926B） | — |

## B. 経路 × テスト

| 経路 | 正常 | 負例 | CTest / 単体 |
| --- | --- | --- | --- |
| bearer 上限表 | U6/SIMULATED=926B admit | 927B `REJECTED` / `INVALID_PAYLOAD_LENGTH`、TX marker 不変 | `v1_runtime_capability` `test_bearer_limit_table` / `test_payload_max_*` |
| logical fragment（単一） | fragment_index=0, count=1 | — | `test_logical_fragment_single` |
| payload admission 境界 | 925B / 926B admit + TX marker=1 | 927B reject、ownership/TX/reservation 増分 0 | `test_payload_max_minus_one_admitted` 他 |
| priority 配送 | DESIRED_STATE(8) が EVENT_FACT(3) より先 | — | `test_priority_dispatch_order` |
| deadline 超過 | step 成功（retry 経路） | — | `test_deadline_timeout_outcome` |
| retry budget 枯渇 | step 成功（terminal outcome） | — | `test_retry_budget_exhaustion` |
| reservation + restart | destroy→re-create 後 TX marker 保持 | — | `test_restart_preserves_admission` |
| simulated bearer loss/delay | path down→enqueue WOULD_BLOCK→path up | — | `test_simulated_bearer_loss_injection` |
| delivery 既存経路（回帰） | desired/event happy + parked/restart | callback failure / false success 禁止 | `v1_runtime_delivery` |
| spine 既存経路（回帰） | submit/cancel admission | — | `v1_runtime_spine` |
| allowlist 拡張 | kind 30 / op 14 gate | gate self-test mutation | `v1_durable_allowlist` / `v1_durable_allowlist_source_gate` |

## C. payload 上限境界（P0-4）

| ケース | payload | 期待 | 実測 |
| --- | ---: | --- | --- |
| max−1 | 925 | `ADMITTED_READY`、TX=1 | Passed |
| max | 926 | `ADMITTED_READY`、TX=1 | Passed |
| max+1 | 927 | `REJECTED`、TX 不変 | Passed |
| 再起動 | 926 admit → destroy → create | TX=1 保持 | Passed |
| partial-apply | 927B reject | storage TX 増分 0 | Passed |

## D. CTest（ASan/UBSan）

```bash
cd tmp-v1
cmake .. -DNINLIL_ENABLE_SANITIZERS=ON -DCMAKE_BUILD_TYPE=Debug
cmake --build . -j8
ctest
```

unit 4 関連（2026-07-23 実行）:

| CTest | 結果 |
| --- | --- |
| v1_runtime_capability | Passed |
| v1_runtime_delivery | Passed |
| v1_runtime_spine | Passed |
| v1_durable_allowlist | Passed |
| v1_durable_allowlist_source_gate | Passed |
| v1_durable_allowlist_gate_self_test | Passed |
| runtime_private_subproject_smoke | Passed（private sources=40） |

```bash
ctest -N | tail -1
# Total Tests: 238
```

## E. allowlist 追記

正本: `docs/work/2026-07-23-v1-durable-allowlist.md` §2c（kind 30 `SPINE_RESERVATION`、op 14 `RESERVATION_COMMIT`）

## F. stub/TODO 0（grep）

```bash
rg -i '\bTODO\b|\bFIXME\b|\bstub\b' \
  src/runtime/runtime_v1_capability.c \
  src/runtime/runtime_v1_capability.h \
  src/runtime/runtime_v1_delivery_durable.c \
  src/runtime/runtime_v1_spine_durable.c \
  tests/runtime/v1_runtime_capability_test.c
```

結果: 0 件

## G. 非主張

- wire fragmentation / reassembly（V2）
- public ABI 変更なし
- production bearer 実機（simulated bearer のみ）

RC=0
