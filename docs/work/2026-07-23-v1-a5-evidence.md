# V1-LAB 項目 5（B4 PC間 direct 1-hop E2E）受入証拠

- 作業dir: `/Users/dt/job/LoRa/ninlil-d3s3-implementation`
- build dir: `tmp-v1`
- 前提 barrier: 項目 `1+2+3+4` 完了（`2db9478`）
- 日付: 2026-07-23

## 実装概要

| 層 | 内容 |
|---|---|
| loopback bearer | Unix domain socket 1-hop、`SO_NOSIGPIPE`、listen non-blocking 再 accept |
| inject 層 | 決定論的 DROP_DATA（送信側・先頭 1 回）、DROP_ACK（受信側・先頭 RECEIPT 1 回）、DUPLICATE |
| runtime 統合 | `runtime_v1_bearer_wire` + `handle_bearer_receipt_timeout`、controller `pending_dispatch` 保持修正 |
| lab platform | loopback + inject ラップ、test accessor（send/recv/drop count） |
| E2E harness | `tests/runtime/v1_direct_1hop_e2e_test.c` — fork + FD サニタイズ、2 プロセス完全 runtime、シナリオ別 inject（controller のみ） |

## シナリオ × 結果

| ID | シナリオ | 注入 | 判定（controller 側） | 結果 |
|---|---|---|---|---|
| (a) | HAPPY | なし | `inject_recv_count >= 1` | PASS |
| (b) | ACK_LOSS | `DROP_ACK` ×1（受信側） | `drop_count >= 1 && recv_count >= 1` | PASS |
| (c) | DATA_LOSS | `DROP_DATA` ×1（送信側 APPLICATION） | `drop_count >= 1 && send_count >= 1` | PASS |
| (d) | TIMEOUT | deadline 超過（clock advance 6000ms/step） | `g_outcome_satisfied == 0` → 明示 timeout outcome | PASS |
| (e) | RESTART | controller step 4 / endpoint step 8 で `platform_restart` | `restarted && step > 40` | PASS |
| (f) | DUPLICATE | 同一 APPLICATION 複製送信 ×1 | `idle_steps >= 3 && recv_count >= 1`、endpoint `g_delivery_calls == 1` | PASS |

endpoint 側: (d) は delivery 不要で `'O'`（decidable）。その他は `g_delivery_calls >= 1`（重複注入は 1 回のみ）。

## 2-process 実行ログ（抜粋）

```
$ cd tmp-v1 && ./ninlil_v1_direct_1hop_e2e_test
v1_direct_1hop_e2e_test ok

$ ctest -R v1_direct_1hop_e2e --output-on-failure
Test #189: v1_direct_1hop_e2e ...............   Passed    2.92 sec

$ ctest -R v1_runtime_delivery --output-on-failure
Test  #33: v1_runtime_delivery ..............   Passed    0.46 sec
```

## CTest 登録

```
$ ctest -N
Total Tests: 239
```

登録名: `v1_direct_1hop_e2e`（実行ファイル: `ninlil_v1_direct_1hop_e2e_test`）

## stub / TODO

新規経路（`tests/runtime/v1_direct_1hop_e2e_test.c`、`ports/posix/loopback_bearer/*`、`runtime_v1_bearer_wire*`）に `TODO` / `FIXME` / 成功 bypass stub **0**（`rg` 確認）。

## 回帰

- `v1_runtime_delivery`: PASS（`pending_dispatch` 修正込み）
- 項目 5 E2E: PASS（上表 6 シナリオ）
