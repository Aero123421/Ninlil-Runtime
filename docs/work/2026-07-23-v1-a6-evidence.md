# V1-LAB 項目 6（B5-LAB application capability 群）受入証拠

- 作業dir: `/Users/dt/job/LoRa/ninlil-d3s3-implementation`
- build dir: `tmp-v1`
- 規範: `docs/02-application-contracts.md`
- 計画: `docs/work/2026-07-23-ninlil-v1-lab-plan.md` 項目 6
- 日付: 2026-07-23

## 実装概要

| 層 | 内容 |
|---|---|
| family model | `src/model/family_capability_model.h` — uplink/downlink family 判定 |
| runtime family | `runtime_v1_family_capability.c/.h` — 4 family 状態遷移、descriptor 役割行列 |
| target resolver | `runtime_v1_target_resolver.c/.h` — submit target 解決、per-target outcome 集約 |
| admission | `submission_admission.c` — LatestState / Measurement uplink admission（項目 4 上限・origin grant 整合） |
| delivery | `runtime_v1_delivery_durable.c` — uplink endpoint 送信 defer-ingress、複数 family dispatch |
| bearer wire | `runtime_v1_bearer_wire.c` — uplink APPLICATION 送受信、RECEIPT 完了 |
| CTest | `tests/runtime/v1_runtime_family_test.c` — family×遷移×負例（ASan/UBSan ビルド） |
| E2E | `v1_direct_1hop_e2e_test.c` シナリオ 7 — LatestState 2-process 配送（ready 同期 + defer ingress） |

## family × 遷移 × test

| family | 遷移（正常） | 負例 | CTest 関数 | 結果 |
|---|---|---|---|---|
| LatestState | generation 10→12 適用 | generation 11 → `STALE_NOT_APPLIED` | `test_latest_state_apply_and_stale` | PASS |
| MeasurementBatch | batch gen 1,2 受理・保持 | gen 1 再送 → `STALE_NOT_APPLIED` | `test_measurement_batch_retention` | PASS |
| BoundedTransfer | begin→receive partial | 中断後 `may_apply=0`（partial-apply 禁止） | `test_bounded_transfer_partial_apply_zero` | PASS |
| ConfigRevision | stage→validate→commit | 不正 revision 4 → `INVALID_PAYLOAD`；rollback 後 6 を再 commit | `test_config_revision_stage_commit_rollback` | PASS |
| target resolver | 2 target とも SATISFIED → aggregate SATISFIED | 1 target FAILED → aggregate FAILED_DEFINITIVE | `test_target_aggregate` | PASS |
| 複数 ServiceDescriptor | controller へ 2 family 登録・submit 分配 | — | `test_multiple_service_register` | PASS |
| counter-offer | — | `ninlil_offer_accept` → `NINLIL_E_UNSUPPORTED` | `test_offer_accept_unsupported` | PASS |

## E2E（項目 5 ハーネス拡張）

| ID | シナリオ | 経路 | 判定 | 結果 |
|---|---|---|---|---|
| (g) | LATEST_STATE | endpoint submit → uplink bearer → controller `on_delivery`；endpoint receipt 受信 | endpoint `send_count>=1 && recv_count>=1`；controller `g_delivery_calls>=1` | PASS |

同期: controller `sync 'R'` 後、endpoint submit 完了を `direct-1hop-7-ready` で待機してから双方 step（idle ingress による loopback 切断レース回避）。

## CTest 実行

```
$ cd tmp-v1 && ctest -R 'v1_runtime_family|v1_runtime_capability|v1_runtime_delivery|v1_runtime_spine|v1_direct_1hop_e2e' --output-on-failure
Test  #32: v1_runtime_spine .................   Passed
Test  #33: v1_runtime_delivery ..............   Passed
Test  #34: v1_runtime_capability ............   Passed
Test  #35: v1_runtime_family ................   Passed
Test #190: v1_direct_1hop_e2e ...............   Passed

100% tests passed, 0 tests failed out of 5
```

```
$ ctest -N
Total Tests: 240
```

## stub / TODO

項目 6 新規経路（`runtime_v1_family_capability*`、`runtime_v1_target_resolver*`、`v1_runtime_family_test.c`、E2E シナリオ 7）に `TODO` / `FIXME` / 成功 bypass stub **0**（`rg` 確認）。

counter-offer: 生成・保存・acceptance 経路は実装せず `NINLIL_E_UNSUPPORTED`（M1a 正本整合）。

## 回帰

- 項目 1–5 CTest: 変更なし（既存 test 弱化なし）
- public ABI: 変更なし
- `v1_direct_1hop_e2e` シナリオ 1–6: PASS（シナリオ 7 追加後も回帰）
